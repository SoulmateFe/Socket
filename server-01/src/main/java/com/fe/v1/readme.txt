1. 先启动v1的server
2. 再启动v1的两个client，其中一个client在发送消息前sleep了30s
3. 两个client互不影响，都能建立连接

server端使用线程池异步处理IO操作，与client建立连接后会开启一个线程来处理IO请求，所以可以继续处理后续的client的连接
1. 如果没有异步的方式处理IO操作，则server端必须处理完当前连接的IO操作才能继续接收下一个客户端连接